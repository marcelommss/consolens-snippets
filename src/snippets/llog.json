{
  "Log with consolens": {
    "prefix": ["log", "llog","loglens", "lenslog"],
    "body": [
      "loglens$1('$2');",
      "$0"
    ],
    "description": "Log using Consolens. Examples: loglens('This is a log'); loglens({ LOG_TYPE.INFORMATION, 'This is also a log' }); loglens({ LOG_TYPE.ERROR, new Error() });"
  }
}
